export class Position {
  positionId: number;
  positionName: string;

  public constructor(positionId: number, positionName: string) {
    this.positionId = positionId;
    this.positionName = positionName;
  }
}
